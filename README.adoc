= Docker ENV demo

== install dependencies:
	
	$ npm install

or use yarn:

	$ yarn

== run the app:
     
     $ npm start
     $ DEBUG=Totally npm start
     $ DEBUG=WhyNot npm start

Then visit http://localhost:3000
